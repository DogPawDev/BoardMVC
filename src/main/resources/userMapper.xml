<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="spring.demo.mappers.UserMapper">
   
    <insert id="insertUser" >
INSERT INTO user_info(user_id,user_nickname) VALUES(#{user_id},#{user_nickname})
    </insert>
</mapper>